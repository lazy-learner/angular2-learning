/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {DropdownQuestionModel} from './dropdown-question-model';

describe('DropdownQuestionModel', () => {
  it('should create an instance', () => {
    expect(new DropdownQuestionModel()).toBeTruthy();
  });
});
